# STM32C011 低功耗开发清单（可复用）

> 目标：后续任何 C011 项目可直接套用，减少“时钟/低功耗/唤醒/RF 线状态”反复踩坑。

---

## 1) 今天验证通过的关键结论

- 欠压进入 `STOP` + `RTC Alarm` 周期唤醒链路稳定：日志显示 `RTC arm currSec -> nextSec` 连续推进，`flashPD ON/OFF` 成对出现。
- 2.4G 模块（XL2400T）在“浅睡眠窗口”仍可能是百 uA 级，但进入更深低功耗后可到 uA 级，这是正常的功耗层级差异。
- 欠压低功耗入口对 RF 的处理必须“硬化”：  
  - 无条件下发 `RF_Link_Sleep()`（不要只依赖软件状态变量）  
  - 进入低功耗前把 `RF_CSN/SCK/DATA` 固定到安全电平（CSN=高，SCK=低，DATA=低）

---

## 2) RCC/时钟配置（本项目稳定组合）

### 2.1 主时钟与外设时钟

- `SYSCLK`：`HSE`（外部晶振）  
- `RTC`：`LSI`（用于低功耗周期唤醒）  
- `HSIKER`：`/4` 给 ADC（降低 ADC 时钟功耗并保持采样稳定）

### 2.2 为什么这样配

- `RTC` 用 `LSI/LSE` 才能在 `STOP/Standby` 里稳定唤醒（这是硬约束）。
- 主频不追求高性能时，`HSE + HSIKER分频`比“全速跑”更可控，尤其对低功耗和 ADC 噪声更友好。

### 2.3 关于“有没有 PLL”

- STM32C0 系列里是否使用 PLL，要看具体芯片与配置需求；本项目当前稳定方案是 **直接 HSE 作系统时钟**，不依赖复杂 PLL 链路。
- 实战建议：先用最短时钟链路跑通低功耗，再考虑提频。

---

## 3) 低功耗模式选择建议

- `SLEEP/WFI`：CPU 休眠，外围保持较多，电流下降有限（常见百 uA 到 mA 级，取决于外设/IO）。
- `STOP`：可在保留上下文下显著降功耗，适合周期唤醒采样/判断。
- `Standby`：最低功耗之一，但唤醒后等价复位启动；适合“深睡 + 复位恢复”架构。

---

## 4) 中断与唤醒处理要点（和“普通跑法”不同的地方）

- `STOP` 唤醒后必须恢复系统时钟与外设时钟（例如 `SystemClock_Config()`、`PeriphCommonClock_Config()`）。
- `RTC_IRQHandler()` 必须调用 `HAL_RTC_AlarmIRQHandler(&hrtc)`，否则 Alarm 事件不会被 HAL 正确消费。
- `EXTI0_1_IRQHandler()` 若用于太阳能上升沿唤醒，必须清中断并保证 PA0 在低功耗前配置正确。
- `Standby` 唤醒是复位流程，不是“从函数返回继续跑”；启动路径要可重入。

---

## 5) RF（XL2400T）最容易踩坑清单

1. **只发 Sleep 指令，不管 IO 电平**  
   - 结果：低功耗后 RF 可能被线状态漂移唤醒，出现“看起来睡了，电流却上去”。
2. **依赖 `g_rf_sleeping` 状态位判断是否发 Sleep**  
   - 结果：状态位与真实硬件态不同步时会漏发睡眠命令。
3. **低功耗前后未统一 RF 三线基准电平**  
   - 结果：不同功耗模式切换时容易出随机问题（电流、通信都不稳定）。

---

## 6) 推荐验收步骤（每次改低功耗都跑一遍）

1. 上电后先验证常规通信可用（RF init/rx 正常）。
2. 触发欠压，观察是否进入预期低功耗分支。
3. 记录 3 个电流平台：  
   - 低功耗准备窗口  
   - 进入低功耗瞬间  
   - 周期唤醒后稳定段
4. 检查日志中 `RTC arm` 秒数是否连续递进，确认没有“假唤醒/卡死”。
5. 低功耗退出后再次验证 RF 通信是否恢复。

---

## 7) 当前项目建议

- 你现在的 STOP 版本（保留 `PWR_FLASHPD_STOP`）是更稳妥的工程落点。  
- 后续若再尝试深度睡眠，先保留这份清单做门禁：任何一项不满足都不要合并到主线。

